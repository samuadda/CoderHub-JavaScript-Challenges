//rectangle area

function area_of_rectangle(width, height) {
    // Calculate and return the area of the rectangle, which is the product of width and height.
    return width * height;
}

// Example usage:
const rectangleArea = area_of_rectangle(5, 4);
console.log("Rectangle Area: " + rectangleArea);
