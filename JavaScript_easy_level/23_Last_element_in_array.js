function last_elm(arr) {
    // Calculate the index of the last element in the array
    let LastElement = arr.length - 1;

    // Return the last element by accessing it using the calculated index
    return arr[LastElement];
}
