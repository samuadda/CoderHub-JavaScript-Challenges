function is_same(name1, name2) {
    // Check if name1 is equal to name2, If they are equal, return 'متشابهتين' (similar) if not return 'غير متشابهتين' (not similar)
    return name1 === name2 ? "متشابهتين" : "غير متشابهتين";
}
